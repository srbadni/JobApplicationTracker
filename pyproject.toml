[tool.pytest.ini_options]
addopts = "-ra --strict-markers"
testpaths = ["api", "core", "db", "features"]

[tool.ruff]
target-version = "py314"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
